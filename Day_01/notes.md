What is python?- Python is a high-level, interpreted, dynamically typed programming language known for its simple syntax, readability, and extensive standard library. It is widely used in web development, automation, data analysis, artificial intelligence, machine learning, and many other fields.

Why is Python popular in AI? - because it is easy to understand and use, it is an open source language, it has got extensive libraries like NumPy, Pandas, Scikit-learn, TensorFlow, and PyTorch, a large developer community, and excellent support for data processing and rapid prototyping.

Difference between List and Tuple.- list is a mutable datatype,uses [],slightly slower and has more memory where as tuple is immutable, uses (), slightly faster and has less memory.

Difference between List and Set. - list allows duplicates and preserves order where as sets doesn't allow duplicates and is unordered.

Mutable vs Immutable. - datatype which can be changed later after its creation is called mutable datatpe where as the ones which cannot be altered once created is calle immutable datatype.